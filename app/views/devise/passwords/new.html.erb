<div class='container login-box'>
  <div class='panel panel-default'>
    <div class='panel-heading'><%= t '.title' %></div>
    <div class='panel-body'>
      <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { role: 'form', method: :post, class: 'form-horizontal' }) do |f| %>
        <p><%= t '.par1' %></p>
        <%= f.input :email, autofocus: true %>
        <div class='form-group'>
          <div class='col-sm-offset-3 col-sm-9'>
            <%= f.submit t('.submit_text'), class: 'btn btn-primary' %>
          </div>
        </div>
        <hr>
        <p>
          <span><%= t '.par2' %></span>
          <strong><%= link_to 'Login', new_session_path(resource_name) %> </strong>
        </p>
      <% end %>
    </div>
  </div>
</div>
